[phases.setup]
nixPkgs = ["tesseract"]

[phases.build]
cmds = ["apt-get update && apt-get install -y tesseract-ocr-hin && rm -rf /var/lib/apt/lists/*"]

[start]
cmd = "uvicorn app.main:app --host 0.0.0.0 --port $PORT"